
import { limitResults } from 'global-search/helpers/limit-results';
import { module, test } from 'qunit';

module('Unit | Helper | limit results');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = limitResults([42]);
  assert.ok(result);
});

