import fauxSearchResult from 'global-search/utils/faux-search-result';
import { module, test } from 'qunit';

module('Unit | Utility | faux search result');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fauxSearchResult();
  assert.ok(result);
});
